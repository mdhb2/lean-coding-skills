{
  "permissions": {
    "allow": [
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path files = sorted(Path(\"skills\").rglob(\"SKILL.md\")) print(\"=== FENCE BALANCE & CoT/HANDOFF ORDER ===\") for f in files: text = f.read_text(encoding=\"utf-8\") fences = len(re.findall(r\"^```\", text, re.MULTILINE)) # Handle 4-backtick fences too fences4 = len(re.findall(r\"^````\", text, re.MULTILINE)) balanced = (fences % 2 == 0) and (fences4 % 2 == 0) # CoT before Handoff check cot_pos = text.find(\"## Chain of Truth Report\") handoff_pos = text.find(\"## Handoff\") cot_before = cot_pos != -1 and handoff_pos != -1 and cot_pos < handoff_pos cot_exists = cot_pos != -1 handoff_exists = handoff_pos != -1 flags = [] if not balanced: flags.append(\"FENCE-UNBALANCED\") if not cot_before and handoff_exists: flags.append(\"COT-AFTER-HANDOFF\") if not cot_exists: flags.append(\"NO-COT\") if flags: print(f\"{f}: {' '.join(flags)}\") print() print(\"=== NAME FIELD VALIDATION (kebab-case) ===\") for f in files: text = f.read_text(encoding=\"utf-8\") m = re.match(r\"^---\\s*\\nname:\\s*(.+?)(?:\\n|$)\", text) if m: name = m.group(1).strip().strip('\"') if not re.match(r\"^[a-z0-9-]+$\", name): print(f\"{f}: BAD name -> {name[:80]}\") print() print(\"=== H1 TITLE DUPLICATES ===\") for f in files: text = f.read_text(encoding=\"utf-8\") h1s = re.findall(r\"^# (.+)$\", text, re.MULTILINE) dupes = [h for h in h1s if h1s.count(h) > 1] if dupes: print(f\"{f}: dup H1 -> {dupes}\") print() print(\"=== DUPLICATE H2 SECTIONS ===\") for f in files: text = f.read_text(encoding=\"utf-8\") h2s = re.findall(r\"^## (.+)$\", text, re.MULTILINE) dupes = [h for h in h2s if h2s.count(h) > 1] if dupes: print(f\"{f}: dup H2 -> {dupes}\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path skill_dirs = sorted(d.name for d in Path(\"skills\").iterdir() if d.is_dir() and d.name.startswith(\"lcs-\")) print(\"SKILL DIRS:\", len(skill_dirs)) for s in skill_dirs: print(\" \", s) # Registry in contract.md vs okf-schema.md vs validate-okf.py contract = Path(\"skills/lcs-shared/contract.md\").read_text() okf = Path(\"skills/lcs-shared/templates/okf-schema.md\").read_text() vokf = Path(\"skills/lcs-shared/scripts/validate-okf.py\").read_text() registry_types = { \"contract\": set(re.findall(r\"\\| `(\\w+)` \\|\", contract)), \"okf\": set(re.findall(r\"\\| `(\\w+)` \\|\", okf)), \"validate-okf\": set(re.findall(r'\"(\\w+)\"', vokf.split(\"VALID_ARTIFACT_TYPES = {\")[1].split(\"}\")[0])), } # only artifact types import json valid_artifact_types = {\"explore\",\"prd\",\"prd_enhanced\",\"srs\",\"tests\",\"api\",\"db\",\"traceability\",\"task_coverage\",\"task\",\"debug\",\"debug_ext\",\"code_review\",\"codebase_doc\",\"onboarding\",\"onboarding_map\",\"final_doc\",\"final_map\",\"analysis\",\"session_log\",\"domain_model\",\"research\",\"prototype\",\"wayfinder\",\"wizard\",\"execution_log\"} print(\"\\n=== ARTIFACT TYPE REGISTRY COVERAGE ===\") for k, v in registry_types.items(): missing = valid_artifact_types - v extra = v - valid_artifact_types print(f\"{k}: has {len(v)} types; MISSING from registry: {sorted(missing) if missing else 'none'}; EXTRA: {sorted(extra) if extra else 'none'}\") # templates exist for registry types? templates = set(p.name.replace(\".template.md\",\"\") for p in Path(\"skills/lcs-shared/templates\").glob(\"*.template.md\")) print(\"\\nTemplates dir has:\", sorted(templates)) print(\"Missing templates for registered types:\", sorted(valid_artifact_types - templates)) EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path skill_dirs = sorted(d.name for d in Path(\"skills\").iterdir() if d.is_dir() and d.name.startswith(\"lcs-\")) # Collect all references to lcs-* skills across all SKILL.md + templates + contract refs = {} for f in list(Path(\"skills\").rglob(\"SKILL.md\")) + list(Path(\"skills/lcs-shared\").rglob(\"*.md\")): text = f.read_text(encoding=\"utf-8\") found = set(re.findall(r\"lcs-[a-z0-9-]+\", text)) for s in found: refs.setdefault(s, set()).add(str(f)) print(\"=== REFERENCED SKILLS NOT ON DISK ===\") for s in sorted(refs): if s not in skill_dirs: files = \", \".join(sorted(refs[s])[:4]) print(f\" {s} <- {files}\") print(\"\\n=== SKILLS ON DISK NEVER REFERENCED BY OTHERS ===\") all_mentioned = set(refs.keys()) for s in sorted(skill_dirs): if s not in all_mentioned: print(f\" {s}\") print(\"\\n=== SELF-REFERENCE LOOPS ===\") for s in sorted(skill_dirs): selfrefs = [f for f in refs.get(s, set()) if s in str(f)] if selfrefs: print(f\" {s}: self-references in {len(selfrefs)} file(s)\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path print(\"=== PATH CONVENTION USAGE PER SKILL ===\") for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") paths = set(re.findall(r\"\\.lcs/[a-z0-9\\-/{}\\.]+\", text)) if paths: print(f\"\\n{f.parent.name}:\") for p in sorted(paths): print(f\" {p}\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path for name in [\"lcs-master\", \"lcs-chain-of-truth\", \"lcs-debug-ext\", \"lcs-domain-modeling\", \"lcs-research\", \"lcs-prototype\", \"lcs-wayfinder\", \"lcs-wizard\"]: f = Path(f\"skills/{name}/SKILL.md\") text = f.read_text(encoding=\"utf-8\") fences = len(re.findall(r\"^```\", text, re.MULTILINE)) cot_pos = text.find(\"## Chain of Truth Report\") handoff_pos = text.find(\"## Handoff\") cot_before = cot_pos != -1 and handoff_pos != -1 and cot_pos < handoff_pos h2s = re.findall(r\"^## (.+)$\", text, re.MULTILINE) dupes = [h for h in h2s if h2s.count(h) > 1] flags = [] if fences % 2 != 0: flags.append(\"FENCE-UNBALANCED\") if not cot_before and handoff_pos != -1: flags.append(\"COT-AFTER-HANDOFF\") if dupes: flags.append(f\"DUP-H2:{dupes}\") print(f\"{name}: {'OK' if not flags else ' '.join(flags)}\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path def strip_fences(text): \"\"\"Remove content inside code fences to avoid false positives.\"\"\" lines = text.splitlines() out = [] in_fence = False for line in lines: if re.match(r\"^```\", line): in_fence = not in_fence continue if not in_fence: out.append(line) return \"\\n\".join(out) for name in [\"lcs-chain-of-truth\", \"lcs-debug-ext\"]: f = Path(f\"skills/{name}/SKILL.md\") text = f.read_text(encoding=\"utf-8\") body = strip_fences(text) cot_pos = body.find(\"## Chain of Truth Report\") handoff_pos = body.find(\"## Handoff\") handoff_behav = body.find(\"## Handoff Behavior\") # find exact heading lines headings = re.findall(r\"^## (.+)$\", body, re.MULTILINE) dupes = [h for h in headings if headings.count(h) > 1] cot_before = cot_pos != -1 and handoff_pos != -1 and cot_pos < handoff_pos print(f\"{name}: headings={headings}\") print(f\" COT before real Handoff: {cot_before}\") if dupes: print(f\" DUP-H2: {dupes}\") EOF)",
      "Shell(node scripts/validate-skills.js 2 >& 1)",
      "Shell(python3 skills/lcs-shared/scripts/validate-okf.py skills/ 2 >& 1)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path print(\"=== Handoff sections missing SOT Bundle or Must Preserve IDs ===\") for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") # Find the last Handoff section m = re.search(r\"^## Handoff\\s*$([\\s\\S]*?)(?=^## |\\Z)\", text, re.MULTILINE) if not m: continue handoff = m.group(1) missing = [] if \"Source of Truth Bundle\" not in handoff: missing.append(\"SOT Bundle\") if \"Must Preserve IDs\" not in handoff: missing.append(\"Must Preserve IDs\") if missing: print(f\"{f.parent.name}: missing {' + '.join(missing)}\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path print(\"=== FENCE BALANCE CHECK ===\") issues = 0 for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") fences = len(re.findall(r\"^```\", text, re.MULTILINE)) if fences % 2 != 0: print(f\"{f}: UNBALANCED ({fences} fences)\") issues += 1 print(f\"Total issues: {issues}\" if issues else \"All fences balanced.\") print(\"\\n=== DUPLICATE H2 CHECK ===\") issues = 0 for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") # strip code fences to avoid false positives lines = [] in_fence = False for line in text.splitlines(): if re.match(r\"^```\", line): in_fence = not in_fence continue if not in_fence: lines.append(line) h2s = re.findall(r\"^## (.+)$\", \"\\n\".join(lines), re.MULTILINE) dupes = [h for h in h2s if h2s.count(h) > 1] if dupes: print(f\"{f}: dup H2 -> {dupes}\") issues += 1 print(f\"Total issues: {issues}\" if issues else \"No duplicate H2 sections.\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path skill_dirs = sorted(d.name for d in Path(\"skills\").iterdir() if d.is_dir() and d.name.startswith(\"lcs-\")) refs = {} for f in list(Path(\"skills\").rglob(\"SKILL.md\")) + list(Path(\"skills/lcs-shared\").rglob(\"*.md\")): text = f.read_text(encoding=\"utf-8\") found = set(re.findall(r\"lcs-[a-z0-9-]+\", text)) for s in found: refs.setdefault(s, set()).add(str(f)) print(\"=== REFERENCED SKILLS NOT ON DISK (excl. debug prefix) ===\") for s in sorted(refs): if s not in skill_dirs and not s.startswith(\"lcs-7f3a\"): files = \", \".join(sorted(refs[s])[:4]) print(f\" {s} <- {files}\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path # Check referenced file paths from SKILL.md files missing = [] for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") skill_dir = f.parent # find relative file references for m in re.finditer(r\"`([^`]+)`\", text): ref = m.group(1) # filter for plausible relative paths if ref.startswith((\"references/\", \"assets/\", \"scripts/\", \"templates/\")): target = skill_dir / ref # also check shared if not target.exists(): shared_target = Path(\"skills/lcs-shared\") / ref if not shared_target.exists(): missing.append(f\"{f.parent.name}: missing {ref}\") # check $SKILL_ROOT refs for m in re.finditer(r\"\\$SKILL_ROOT/([^\\s`]+)\", text): target = skill_dir / m.group(1) if not target.exists(): missing.append(f\"{f.parent.name}: missing $SKILL_ROOT/{m.group(1)}\") print(\"Missing referenced files:\" if missing else \"All referenced files exist.\") for x in sorted(set(missing)): print(\" \", x) EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && ls skills/lcs-wizard/ skills/lcs-shared/templates/wizard.template.md 2>&1; echo \"---\"; grep -n \"template.sh\\|scripts/\" skills/lcs-wizard/SKILL.md skills/lcs-shared/templates/wizard.template.md 2>&1)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && ls skills/lcs-debug-ext/scripts/ 2>&1; echo \"---\"; bash -n skills/lcs-wizard/template.sh && echo \"template.sh syntax OK\")",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path print(\"=== FINAL MARKDOWN INTEGRITY ===\") issues = 0 for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") fences = len(re.findall(r\"^```\", text, re.MULTILINE)) if fences % 2 != 0: print(f\"{f}: UNBALANCED fences\") issues += 1 # name field m = re.match(r\"^---\\s*\\nname:\\s*(.+?)(?:\\n|$)\", text) if m: name = m.group(1).strip().strip('\"') if not re.match(r\"^[a-z0-9-]+$\", name): print(f\"{f}: BAD name field\") issues += 1 # dup H2 (outside fences) lines = [] in_fence = False for line in text.splitlines(): if re.match(r\"^```\", line): in_fence = not in_fence continue if not in_fence: lines.append(line) h2s = re.findall(r\"^## (.+)$\", \"\\n\".join(lines), re.MULTILINE) dupes = [h for h in h2s if h2s.count(h) > 1] if dupes: print(f\"{f}: DUP H2 {dupes}\") issues += 1 # CoT Level declaration if not re.search(r\"## Chain of Truth Level\", text): print(f\"{f}: NO CoT Level declaration\") issues += 1 print(f\"Total issues: {issues}\" if issues else \"All clean.\") print(\"\\n=== FRONTMATTER NAME CHECK ===\") for f in sorted(Path(\"skills\").rglob(\"SKILL.md\")): text = f.read_text(encoding=\"utf-8\") m = re.match(r\"^---\\s*\\nname:\\s*(.+?)(?:\\n|$)\", text) folder = f.parent.name name = m.group(1).strip().strip('\"') if m else \"?\" if name != folder: print(f\" MISMATCH: folder={folder} name={name}\") print(\"All folder names match frontmatter name fields.\") EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path skill_dirs = sorted(d.name for d in Path(\"skills\").iterdir() if d.is_dir() and d.name.startswith(\"lcs-\")) for fname in [\"README.md\", \"README-ID.md\"]: text = Path(fname).read_text(encoding=\"utf-8\") mentioned = set(re.findall(r\"`(lcs-[a-z0-9-]+)`\", text)) missing = sorted(set(skill_dirs) - mentioned) extra = sorted(mentioned - set(skill_dirs)) # check each skill has a documented section with scenario h2_doc_skills = set(re.findall(r\"^#### `(lcs-[a-z0-9-]+)`\", text, re.MULTILINE)) no_section = sorted(set(skill_dirs) - h2_doc_skills - {\"lcs-shared\"}) print(f\"{fname}:\") print(f\" skills on disk: {len(skill_dirs)}\") print(f\" mentioned in text: {len(mentioned)}\") print(f\" missing from text: {missing if missing else 'none'}\") print(f\" extra mentions: {extra if extra else 'none'}\") print(f\" skills without doc section: {no_section if no_section else 'none'}\") print() EOF)",
      "Shell(cd /mnt/Data/Workspace/ubx-devbox/projects/lean-coding-skills && python3 - <<'EOF' import re from pathlib import Path for fname in [\"README.md\", \"README-ID.md\"]: text = Path(fname).read_text(encoding=\"utf-8\") fences = len(re.findall(r\"^```\", text, re.MULTILINE)) h2 = re.findall(r\"^## (.+)$\", text, re.MULTILINE) h4 = len(re.findall(r\"^#### \", text, re.MULTILINE)) print(f\"{fname}: {len(text.splitlines())} lines, fences balanced={fences%2==0}, H2 sections={len(h2)}, skill doc sections={h4}\") print(f\" H2: {h2}\") print() EOF)",
      "Shell(rm:*)"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}